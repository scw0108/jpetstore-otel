apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: jpetstore-otel-test
  annotations:
    run.googleapis.com/ingress: all
    run.googleapis.com/ingress-status: all
  labels:
    cloud.googleapis.com/location: asia-east1
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '4'
        autoscaling.knative.dev/minScale: '1'
        run.googleapis.com/cpu-throttling: 'false'
        run.googleapis.com/startup-cpu-boost: 'true'
      labels:
        run.googleapis.com/startupProbeType: Custom
    spec:
      containerConcurrency: 100
      timeoutSeconds: 300
      # serviceAccountName: opentelemetry-demo@das-ct-lab.iam.gserviceaccount.com
      containers:
        - name: jpetstore-app
          image: asia-east1-docker.pkg.dev/das-ct-lab/test-otel/jpetstore
          ports:
            - containerPort: 8080
          env:
            - name: spring.profiles.active
              value: test,gcp
            - name: logging.level.com.example.otel
              value: debug
            - name: JAVA_TOOL_OPTIONS
              value: -Djava.security.egd=file:/dev/./urandom -Dfile.encoding=UTF-8
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://localhost:4317"
            - name: OTEL_SERVICE_NAME
              value: "jpetstore-otel-demo"
            - name: OTEL_TRACES_SAMPLER
              value: "always_on"
            - name: OTEL_METRICS_EXPORTER
              value: "otlp"
            - name: OTEL_LOGS_EXPORTER
              value: "otlp"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=lab,service.name=jpetstore-otel-test"
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
            failureThreshold: 10
            timeoutSeconds: 1
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 180
            failureThreshold: 10
            timeoutSeconds: 1
        - name: otel-collector
          image: asia-east1-docker.pkg.dev/das-ct-lab/test-otel/collector:0.0.3
          resources:
            limits:
              cpu: 500m
              memory: 128Mi
  traffic:
    - percent: 100
      latestRevision: true
